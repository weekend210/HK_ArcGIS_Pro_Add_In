<Window x:Class="HK_AREA_SEARCH.Views.CustomIntervalDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:HK_AREA_SEARCH.Views"
        mc:Ignorable="d" 
        Title="{Binding Title}" Height="450" Width="600"
        WindowStartupLocation="CenterOwner">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <DataGrid Grid.Row="0" 
                  ItemsSource="{Binding ClassItems}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="开始值" 
                                    Binding="{Binding StartValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                    Width="*"/>
                <DataGridTextColumn Header="结束值" 
                                    Binding="{Binding EndValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                    Width="*"/>
                <DataGridTextColumn Header="类别值" 
                                    Binding="{Binding ClassValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                    Width="*"/>
                <DataGridTemplateColumn Header="操作" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="删除" 
                                    Command="{Binding RelativeSource={RelativeSource AncestorType=DataGrid}, 
                                            Path=DataContext.RemoveClassCommand}" 
                                    CommandParameter="{Binding}" 
                                    Width="60" Height="25"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="添加分类" 
                    Command="{Binding AddClassCommand}" 
                    Width="80" Height="25" 
                    Margin="0,0,10,0"/>
            <Button Content="确定" 
                    Command="{Binding OKCommand}" 
                    Width="60" Height="25" 
                    Margin="0,0,5,0"/>
            <Button Content="取消" 
                    Command="{Binding CancelCommand}" 
                    Width="60" Height="25"/>
        </StackPanel>
    </Grid>
</Window>